# El entorno que trae VasakOS. Se carga desde acá para que las mejoras del
# escritorio lleguen sin tocar este archivo, que es tuyo. Lo que escribas debajo
# gana, porque corre después.
[ -r /usr/share/vasak/shell/profile ] && . /usr/share/vasak/shell/profile

# Lo tuyo va acá abajo.
